import React from 'react';
import { ComingNext } from '../components/ui/ComingNext';

export default function Delivery() {
  return (
    <ComingNext
      title="Delivery"
      note="Your delivered photos and edits will live here — including the 1 free revision per order — once the delivery pipeline ships (Phase 3)."
    />
  );
}
