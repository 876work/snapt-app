import React from 'react';
import { ComingNext } from '../../components/ui/ComingNext';

export default function SessionDay() {
  return (
    <ComingNext
      title="Session day"
      note="Safety-code check-in, in-session chat, session sharing, and the SOS action (visible only while a session is active) arrive with the trust & safety build (Phase 4)."
    />
  );
}
