import React from 'react';
import { ComingNext } from '../components/ui/ComingNext';

export default function Upload() {
  return (
    <ComingNext
      title="Upload footage"
      note="The remote-edit order flow — upload, choose your edit, order summary, and status tracking — ships with the footage pipeline (Phase 3)."
    />
  );
}
